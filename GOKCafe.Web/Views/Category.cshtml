@using GOKCafe.Web.Models.ViewModels
@model CategoryViewModel
@{
    Layout = null;
    ViewData["Title"] = Model.Category.Name;
}

<div class="category-page">
    <!-- Category Header -->
    <section class="category-header bg-light py-5">
        <div class="container text-center">
            @if (!string.IsNullOrEmpty(Model.Category.ImageUrl))
            {
                <img src="@Model.Category.ImageUrl" alt="@Model.Category.Name" class="category-header-image mb-3 rounded-circle" style="width: 150px; height: 150px; object-fit: cover;" />
            }
            <h1 class="display-4">@Model.Category.Name</h1>
            @if (!string.IsNullOrEmpty(Model.Category.Description))
            {
                <p class="lead">@Model.Category.Description</p>
            }
            <p class="text-muted">@Model.Category.ProductCount Products</p>
        </div>
    </section>

    <!-- Category Products -->
    <section class="py-5">
        <div class="container">
            @if (Model.Products.Items.Any())
            {
                <div class="product-grid">
                    @foreach (var product in Model.Products.Items)
                    {
                        <div class="product-card">
                            <div class="product-image-wrapper">
                                @if (!string.IsNullOrEmpty(product.ImageUrl))
                                {
                                    <img src="@product.ImageUrl" alt="@product.Name" class="product-image" />
                                }
                                else
                                {
                                    <div class="product-image-placeholder">
                                        <i class="fas fa-coffee fa-3x"></i>
                                    </div>
                                }
                                @if (product.DiscountPrice.HasValue && product.DiscountPrice > 0)
                                {
                                    <span class="badge badge-sale">Sale</span>
                                }
                            </div>

                            <div class="product-info">
                                <h3 class="product-name">
                                    <a href="/products/@product.Slug">@product.Name</a>
                                </h3>

                                @if (!string.IsNullOrEmpty(product.ShortDescription))
                                {
                                    <p class="product-description">@product.ShortDescription</p>
                                }

                                <div class="product-price">
                                    @if (product.DiscountPrice.HasValue && product.DiscountPrice > 0)
                                    {
                                        <span class="price-original">$@product.Price.ToString("F2")</span>
                                        <span class="price-discount">$@product.DiscountPrice.Value.ToString("F2")</span>
                                    }
                                    else
                                    {
                                        <span class="price">$@product.Price.ToString("F2")</span>
                                    }
                                </div>

                                <div class="product-actions mt-3">
                                    <a href="/products/@product.Slug" class="btn btn-outline-primary btn-sm">
                                        View Details
                                    </a>
                                    <button class="btn btn-primary btn-sm" onclick="addToCart('@product.Id')">
                                        <i class="fas fa-cart-plus"></i>
                                    </button>
                                </div>
                            </div>
                        </div>
                    }
                </div>

                <!-- Pagination -->
                @if (Model.Products.TotalPages > 1)
                {
                    <nav aria-label="Product pagination" class="mt-5">
                        <ul class="pagination justify-content-center">
                            @if (Model.Products.HasPreviousPage)
                            {
                                <li class="page-item">
                                    <a class="page-link" href="?page=@(Model.Products.PageNumber - 1)">Previous</a>
                                </li>
                            }

                            @for (int i = 1; i <= Model.Products.TotalPages; i++)
                            {
                                <li class="page-item @(i == Model.Products.PageNumber ? "active" : "")">
                                    <a class="page-link" href="?page=@i">@i</a>
                                </li>
                            }

                            @if (Model.Products.HasNextPage)
                            {
                                <li class="page-item">
                                    <a class="page-link" href="?page=@(Model.Products.PageNumber + 1)">Next</a>
                                </li>
                            }
                        </ul>
                    </nav>
                }
            }
            else
            {
                <div class="alert alert-info text-center">
                    <h4>No products in this category yet</h4>
                    <p>Check back soon for new products!</p>
                    <a href="/products" class="btn btn-primary">View All Products</a>
                </div>
            }
        </div>
    </section>
</div>

@section Scripts {
    <script>
        function addToCart(productId) {
            console.log('Add to cart:', productId);
            alert('Product added to cart!');

            const cartCount = document.querySelector('.cart-count');
            if (cartCount) {
                const currentCount = parseInt(cartCount.textContent) || 0;
                cartCount.textContent = currentCount + 1;
            }
        }
    </script>
}