@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage
@{
    // Get the OurDelicious parent node from Home
    var ourDeliciousSection = UmbracoContext?.Content?.GetAtRoot()
        .FirstOrDefault(x =>  x.ContentType.Alias == "homepage")?
        .Children
        .FirstOrDefault(x => x.ContentType.Alias == "ourDeliciousSection");

    var items = ourDeliciousSection?.Children.Where(x => x.IsVisible()).ToList() ?? new List<Umbraco.Cms.Core.Models.PublishedContent.IPublishedContent>();

    // Fallback mock data if no Umbraco content
    var mockItems = new List<dynamic>();
    if (!items.Any())
    {
        mockItems = new List<dynamic>
        {
            new {
                Title = "Types of Coffee",
                Description = "The four main types of coffee beans have distinct characteristics that determine flavor, caffeine, and cultivation.",
                BackgroundUrl = "/media/image/Delicious1.png",
                Url = "/products"
            },
            new {
                Title = "Bubble Tea",
                Description = "The beverage is typically served cold with a distinct, extra-wide straw that allows the drinker to easily sip and chew the boba pearls.",
                BackgroundUrl = "/media/image/Delicious2.png",
                Url = "/products"
            },
            new {
                Title = "Sweet Doughs",
                Description = "Baked goods made from a dough rich in fat, such as butter or shortening, which gives it a characteristically flaky, crisp, or delicate texture",
                BackgroundUrl = "/media/image/Delicious3.png",
                Url = "/products"
            }
        };
    }
}

@if (items.Any() || mockItems.Any())
{
    <!-- Our Delicious Offer Section -->
    <section class="flex flex-col justify-center py-20 bg-white font-heading">
        <div class="container mx-auto">
            <!-- Section Header -->
            <div class="text-center mb-4 sm:mb-4 lg:mb-4">
                <p class="text-[#0767B3] text-lg font-medium uppercase tracking-wide mb-1">EXPERIENCE OUR BEST</p>
                <h2 class="text-4xl md:text-5xl font-semibold text-gray-900">OUR DELICIOUS OFFER</h2>
            </div>

            <!-- Product Cards Grid -->
            <div class="grid grid-cols-1 md:grid-cols-3 gap-4 sm:gap-5 lg:gap-6 xl:w-full  mx-auto">
                @if (items.Any())
                {
                    @foreach (var item in items)
                    {
                        var title = item.Value<string>("title") ?? item.Name;
                        var description = item.Value<string>("description") ?? "";

                        // Get the background image from Media Picker
                        var backgroundImage = item.Value<Umbraco.Cms.Core.Models.PublishedContent.IPublishedContent>("backgroundImage");
                        var backgroundUrl = backgroundImage?.Url() ?? "";

                        <div class="group relative overflow-hidden shadow-lg h-[600px] sm:h-[450px] lg:h-[500px] xl:h-[600px]">
                            <!-- Background Image with Overlay -->
                            <div class="absolute inset-0">
                                @if (!string.IsNullOrEmpty(backgroundUrl))
                                {
                                    <img src="@backgroundUrl" alt="@title" class="w-full h-full object-cover" loading="lazy" />
                                }
                                else
                                {
                                    <div class="w-full h-full bg-gray-500"></div>
                                }
                                <div class="absolute inset-0 bg-black/30 group-hover:bg-black/40 transition-all duration-300"></div>
                            </div>

                            <!-- Content -->
                            <div class="relative h-full flex flex-col justify-end p-4 sm:p-6 lg:p-8 text-white">
                                <h3 class="text-lg sm:text-xl lg:text-2xl font-semibold mb-2 sm:mb-3">@title</h3>
                                <p class="text-white text-xs sm:text-sm mb-4 sm:mb-5 lg:mb-6 line-clamp-2 sm:line-clamp-3">@description</p>
                                <div>
                                    <a href="@item.Url()" class="inline-block border-2 border-white text-white px-4 sm:px-5 lg:px-6 py-2 sm:py-2.5 lg:py-3 text-sm sm:text-base font-semibold hover:bg-white hover:text-gray-900 transition-all duration-300">
                                        View Products
                                    </a>
                                </div>
                            </div>
                        </div>
                    }
                }
                else
                {
                    @foreach (var item in mockItems)
                    {
                        <div class="group relative overflow-hidden shadow-lg h-[500px] sm:h-[450px] lg:h-[500px] xl:h-[550px]">
                            <!-- Background Image with Overlay -->
                            <div class="absolute inset-0">
                                <img src="@item.BackgroundUrl" alt="@item.Title" class="w-full h-full object-cover" loading="lazy" />
                                <div class="absolute inset-0 bg-black/30 group-hover:bg-black/40 transition-all duration-300"></div>
                            </div>

                            <!-- Content -->
                            <div class="relative h-full flex flex-col justify-end p-4 sm:p-6 lg:p-8 text-white">
                                <h3 class="text-2xl sm:text-xl lg:text-2xl font-semibold mb-2 sm:mb-3">@item.Title</h3>
                                <p class="text-white text-base sm:text-sm mb-4 sm:mb-5 lg:mb-6 line-clamp-4 sm:line-clamp-3">@item.Description</p>
                                <div>
                                    <a href="@item.Url" class="w-full text-center sm:w-[180px] inline-block border-2 border-white text-white px-4 py-4 sm:px-5 lg:px-6 py-2 sm:py-2.5 lg:py-3 text-sm sm:text-base font-semibold hover:bg-white hover:text-gray-900 transition-all duration-300">
                                        View Products
                                    </a>
                                </div>
                            </div>
                        </div>
                    }
                }
            </div>
        </div>
    </section>
}
